import React, { useState } from "react";

export function Favorites() {
    const { handleOnChange, filteredData, searchWord} = useGif()

  return (
    <main className=' justify-start items-center w-full h-full'>

    </main>
  )
}